import { ZerofrontdigitPipe } from './zerofrontdigit.pipe';

describe('ZerofrontdigitPipe', () => {
  it('create an instance', () => {
    const pipe = new ZerofrontdigitPipe();
    expect(pipe).toBeTruthy();
  });
});
